// Export SQLiteNotificationService as the default NotificationService
// This maintains backward compatibility with existing imports
export { default } from './SQLiteNotificationService';
export * from './SQLiteNotificationService';